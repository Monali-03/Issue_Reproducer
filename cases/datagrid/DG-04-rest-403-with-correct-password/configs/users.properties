# Excerpt from the customer's server/conf/users.properties.
# $REALM_NAME=default$
# $ALGORITHM=encrypted$
# Credentials are stored as a DIGEST-MD5 hash of user:realm:password, not in clear text.
# Hash values redacted.
admin=REDACTED_DIGEST_HASH
monitor=REDACTED_DIGEST_HASH
