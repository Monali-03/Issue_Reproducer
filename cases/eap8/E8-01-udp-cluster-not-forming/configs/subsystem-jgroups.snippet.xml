<!-- Excerpt from EAP 8.1 standalone-ha.xml as shipped. NOT a complete server configuration.
     Note: in 8.1 the channel's stack is HARDCODED with no expression, so
     -Djboss.default.jgroups.stack is ignored. The stack must be changed in the XML. -->
<subsystem xmlns="urn:jboss:domain:jgroups:9.0">
    <channels default="ee">
        <channel name="ee" stack="udp" cluster="ejb"/>
    </channels>
    <stacks>
        <stack name="udp">
            <transport type="UDP" socket-binding="jgroups-udp"/>
            <protocol type="dns.DNS_PING"/>
            <protocol type="MERGE3"/>
            <protocol type="FD_SOCK2"/>
            <protocol type="FD_ALL3"/>
            <protocol type="VERIFY_SUSPECT2"/>
            <protocol type="pbcast.NAKACK2"/>
            <protocol type="UNICAST3"/>
            <protocol type="pbcast.STABLE"/>
            <protocol type="pbcast.GMS"/>
            <protocol type="UFC"/>
            <protocol type="MFC"/>
            <protocol type="FRAG4"/>
        </stack>
    </stacks>
</subsystem>
